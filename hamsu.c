#include <stdio.h>
/*int sum(int a, int b){
    return a+b; 매개변수가 있고 반환값도 있는 함수 */

/* void function_withpout_return(){

    printf("반환값이 없는 함수입니다."); 반환값과 매개변수가 둘 다 없는 함수 
}*/
/* int function_with_return(){
    return 11;
} 매개변수는 없고 반환값만 있는 함수 */
/* void only_byeonsu(int a, int b){
    printf("%d", a*b); 
} 반환값은 없고 오로지 매개변수만 있는 함수 */
int main(void)
{
    //printf("%d",sum(3,4));
    //function_withpout_return();
    //printf("%d", function_with_return());
    //only_byeonsu(3,4);

    return 0;
}